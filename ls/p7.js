//wajp to read length and breadth
//and print area and perimeter of rect
let prompt=require("prompt-sync")();
let length=parseInt(prompt("Enter the number 1"));
let length=parseInt(prompt("Enter the number 1"));
